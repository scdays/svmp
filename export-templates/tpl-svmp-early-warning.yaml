# 由《数据外发字段说明》章节「数据外发 —— 预警」生成，请校对 source 映射
exportTemplateId: tpl-svmp-early-warning
displayName: 数据外发 —— 预警
match:
  exportProfile: early-warning
output:
  format: JSON
  encoding: UTF-8
trigger:
  on: TASK_COMPLETED
filter:
  vulInfoStatList: [1, 2, 3, 5, 6, 7, 8, 9, 10]
fields:
  - target: id
    source: id  # 情报 ID
  - target: intell_uuid
    source: intell_uuid  # 情报 UUID
  - target: intell_name
    source: intell_name  # 情报名称
  - target: is_known
    source: is_known  # 是否已知情报
  - target: cvss_score
    source: cvss_score  # C VSS 评分
  - target: risk_level
    source: risk_level  # 风险等 级
  - target: publisher
    source: publisher  # 发布者
  - target: poc_found
    source: poc_found  # 是否发现 POC
  - target: heat
    source: heat  # 热度
  - target: status
    source: status  # 状态
  - target: solution
    source: solution  # 解决方案
  - target: can_deleted
    source: can_deleted  # 是否可 删除
  - target: invalid_time
    source: invalid_time  # 无效时间
  - target: source
    source: source  # 来源
  - target: is_scan
    source: is_scan  # 是否可扫描
  - target: not_confirm
    source: not_confirm  # 未确认情报
  - target: confirm
    source: confirm  # 确认情 报
  - target: all_fixed
    source: all_fixed  # 所有已 修复
  - target: all_waited
    source: all_waited  # 所有待处理
  - target: cve_id
    source: cve_id  # CVE 编号
  - target: is_fusion
    source: is_fusion  # 是否融 合
  - target: vul_ids
    source: vul_ids  # 漏洞 ID 列表
  - target: created_time
    source: created_time  # 创建时间
  - target: url_info
    source: url_info  # /in tell/new/publi sh/new
  - target: app
    source: app  # 版本列表
  - target: ssh
    source: ssh  # 版本列表
  - target: versions
    source: versions  # 版本列表
  - target: version
    source: version  # 版本
  - target: vendor
    source: vendor  # 厂商
  - target: type
    source: type  # 类型
  - target: version_zh
    source: version_zh  # 版本（中文） }}}],
  - target: cwe_id
    source: cwe_id  # CWE 编号
  - target: cnnvd_id
    source: cnnvd_id  # CNNV D 编号
  - target: apache_id
    source: apache_id  # Apache 编号
  - target: bid
    source: bid  # BID 编号
  - target: cisco_id
    source: cisco_id  # Cisc o 编号
  - target: xf_id
    source: xf_id  # XF 编号 } 2.warnin g_info.t xt 预警信息 {
  - target: intell_id
    source: intell_id  # 情报 ID
  - target: warning_type
    source: warning_type  # 预警类型
  - target: trigger
    source: trigger  # 触发器
  - target: asset_uuid
    source: asset_uuid  # 资产 UUID
  - target: match_type
    source: match_type  # 匹配类型
  - target: products
    source: products  # 软 件信息
  - target: name
    source: name  # 软件名 称
  - target: degree
    source: degree  # 影 响程度 } ],
  - target: asset_name
    source: asset_name  # 资产名称
  - target: asset_key
    source: asset_key  # 资产关键字
  - target: importance
    source: importance  # 重要性
  - target: group_id
    source: group_id  # 组 ID
  - target: group_name
    source: group_name  # 组 名称
  - target: asset_type
    source: asset_type  # 资产类型 }, {
