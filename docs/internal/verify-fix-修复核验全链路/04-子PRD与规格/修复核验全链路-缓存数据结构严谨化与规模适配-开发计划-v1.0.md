# 修复核验全链路 - 缓存数据结构严谨化与规模适配 开发计划 v1.0

> **对应 PRD**：[修复核验全链路-缓存数据结构严谨化与规模适配-PRD-v1.0.md](修复核验全链路-缓存数据结构严谨化与规模适配-PRD-v1.0.md)
> **对应技术设计**：[修复核验全链路-缓存一致性与设备分配技术设计-v1.0.md](修复核验全链路-缓存一致性与设备分配技术设计-v1.0.md)
> **日期**：2026-07-24
> **状态**：待执行（待用户「同意执行」后派子 Agent 实施）
> **工程边界**：后端 `project_backend/svmp/vul-pass` + 前端 `project_frontend/asset/asset-newleak-manage`
> **分支**：待定（建议 `feature/cache-data-rigor`，续用 `feature/fix-ledger-log-records` 亦可）
> **提交规范**：遵循 `esmp-rd-standards` GitFlow，每个 Wave = 1~N 个提交，提交格式按规范。

---

## 1. 状态总表

| Wave | 主题 | 对应 PRD | 优先级 | 状态 |
|------|------|----------|--------|------|
| S1 | 预览构建强校验 + 零静默降级 | R-1/R-2/R-6.1 | P0 | 待执行 |
| S2 | 计数正确性 + targetScale 拆分 | R-3 | P1 | 待执行 |
| S3 | 有效性窗口复核 + 部署严谨 | R-7/R-9 | P1 | 待执行 |
| S4 | 规模护栏 + 死字段清理 | R-8/R-6/R-10 | P2 | 待执行 |
| S5 | 文档同步 + 验收 | 全部 | P2 | 待执行 |

**执行顺序**：S1 -> S2 -> S3 -> S4 -> S5（S1 是防御地基，必须先行；S2 依赖 S1 的快照/收窄；S3/S4 相对独立；S5 收尾）。S1 内任务可并行（不同文件），S2.1 是 S2.2~S2.3 前置。

---

## 2. 任务拆分

### Wave S1：预览构建强校验 + 零静默降级（P0）

| 任务 | 文件 | 改动要点 | 测试 |
|------|------|----------|------|
| S1.1 新增预览完整性校验器 | 新建 `domain/pass/service/verify/DispatchPlanIntegrityValidator.java` | 校验：① `group.assetIps` ⊆ `previewContext.assetList` IP 集；② `group.instanceIds` ⊆ `previewContext.vulInstSnapshots` ID 集；③ 同 wave 实例无丢失/无跨批重复（Σ instanceCount == wave 应覆盖数）；④ `deviceRequired` 组 `candidateEngHashes` 非空。失败抛 `DispatchPlanIntegrityException`（带 groupKey + 用户可读提示） | `DispatchPlanIntegrityValidatorTest`：assetIps 越界/instanceIds 越界/实例丢失/实例跨批重复/候选池空 各分支 |
| S1.2 接入预览校验 | `VulScanTaskDomainServiceImpl.saveDispatchPlanToCache`（~:147-180） | sign/computeChecksum 前调用 S1.1 校验器；不通过不写缓存、不返 token | 集成：异常预览不写缓存 |
| S1.3 删除资产空壳兜底 | `VulScanTaskSubDomainServiceImpl.buildSubTaskFromPlanGroup:3372-3375` | 删 `syntheticAssetsFromIps` 调用与方法；`filterAssetsByIps` 空结果抛 `DispatchPlanIntegrityException`（"子任务[{groupKey}]资产范围与工单资产快照不一致，请重新预览"） | 单测：过滤空抛异常不造空壳 |
| S1.4 漏洞范围精确收窄 | `VulScanTaskSubDomainServiceImpl.buildSubTaskFromPlanGroup`（~:3287-3382） | 按 `group.instanceIds` 收窄 `subTask.vulInstList`（替代构造器全量继承）；评估构造器:306 是否取消 `vulInstList` 拷贝 | 单测：subTask.vulInstList == group.instanceIds 对应快照 |
| S1.5 判定切片精确化 | `ScanResultStageSlicer.sliceInstances:34-48` / `slicePorts:29-32` | 改以 `subTask` 精确实例集合为准（优先 `subTask.vulInstList`），IP-only 仅作兜底校验而非主切 | `ScanResultStageSlicerPrecisionTest`：同 IP 不同 srcMethod 组判定集互斥 |
| S1.6 缓存 schemaVersion | `CachedPlan` + `VulScanTaskDomainServiceImpl.dispatchWithCachedPlan` | CachedPlan 增 `schemaVersion`（常量当前=1）；确认时校验，不符抛"预览方案版本已升级，请重新预览" | 单测：版本不符拦截 |

**Wave S1 验收**：R-1/R-2/R-6.1 全部降级点改造；构造异常输入均拦截 + 提示；预览=下发全链路一致；新增单测全绿；`mvn test` 通过。

---

### Wave S2：计数正确性 + targetScale 拆分（P1）

| 任务 | 文件 | 改动要点 | 测试 |
|------|------|----------|------|
| S2.1 快照补 vulId | `VulScanTaskDomainServiceImpl.buildVulInstSnapshots:471-491` + `convertVulInstSnapshots:375-401` | 快照增 `vulId` 字段（从 `VulArchiveInstDO` 取），双向序列化（Integer 存 String 还原，同现有约定） | 单测：vulId round-trip |
| S2.2 group 结构化计数 | `VerifyFixDispatchPlanner` group 构建处 + `DispatchPlanGroup` | 增 `assetCount`（组 IP 对应 assetList assetInfo 去重计数）、`vulNum`（组级去重 vulId 数）；`vulInstCount`/`uniqueIpCount` 复用现有 | 单测：计数 == 数据源 |
| S2.3 subTask 计数按组 | `VulScanTaskSubDomainServiceImpl.buildSubTaskFromPlanGroup` + 构造器:300,304 | `vulNum/prcVulNum` 按组写入（修复订单级继承）；`astNum` 已按组（保留） | 单测：subTask.vulNum == 组级去重 |
| S2.4 移除 targetScale | `DispatchPlanGroup` + `DispatchPlanAssembler` + 前端 `DispatchPlanPreview.vue` | 移除 `targetScale` 字符串；前端改由结构化字段组合展示 | 前端视觉验收 |
| S2.5 vulInstNum 列（D-1） | `vul_scan_task_sub.groovy` DDL + `VulScanTaskSubDO/PO` + 物化 | 若 D-1=a：新增 `vul_inst_num` 列，物化时按组写入 | DDL 升级 + 单测 |

**Wave S2 验收**：R-3；计数组级精确；targetScale 双源消除；前端展示正常。

---

### Wave S3：有效性窗口复核 + 部署严谨（P1）

| 任务 | 文件 | 改动要点 | 测试 |
|------|------|----------|------|
| S3.1 确认时轻量复核 | `VulScanTaskDomainServiceImpl.dispatchWithCachedPlan`（校验后、消费前） | 复核：① 实例数/存在性 + stat 仍在预期集合（计数校验降低开销）；② 分配设备 status=0 且仍在候选池。不过抛"预览后数据已变化，请重新预览" | 集成：stat 变更/设备离线 各拦截 |
| S3.2 tokenSecret fail-fast | `ComplianceProperties`/`ConfirmTokenSigner.init` + 启动校验 | 集群模式未配 `tokenSecret` -> 启动失败；单实例 escape + 告警 | 启动测试 |
| S3.3 dispatchAttemptCount 阈值 | `RedisDispatchPlanCacheRepository`/`InMemoryDispatchPlanCacheRepository` + `dispatchWithCachedPlan` | 接入 `maxDispatchAttempts`（=3），超限删 key + 提示重新预览 | 单测：超限拦截 |

**Wave S3 验收**：R-7/R-9；窗口漂移拦截；部署严谨。

---

### Wave S4：规模护栏 + 死字段清理（P2）

| 任务 | 文件 | 改动要点 | 测试 |
|------|------|----------|------|
| S4.1 缓存体积护栏 | `VulScanTaskDomainServiceImpl.saveDispatchPlanToCache` | 序列化后字节 > `vul.dispatch-plan-cache.maxCacheBytes`（默认 16MB） -> 拒绝预览 + "工单规模超限，请联系管理员缩减范围或分单" | 集成：超大单拒绝 |
| S4.2 前端超大组 IP 分页（D-3） | 前端 `DispatchPlanPreview.vue` + 后端按需拉取接口（若 D-3=a） | 单组 IP > 阈值只回传计数，明细按需分页拉取 | 前端性能验收 |
| S4.3 defaultEngHash 兜底审计 | `PhaseDispatchPreviewBuilder:167,269,508` | `deviceRequired && !deviceMatched` -> 拦截下发（`allDevicesMatched=false`），不静默兜底默认设备 | 单测：未匹配拦截 |
| S4.4 forcedEngHashes 接入（D-2） | `VerifyFixDispatchPlanner`/`PhaseDispatchPreviewBuilder` 候选池过滤 | 候选池经 forcedEngHashes 过滤；空交集 -> 拦截 + 提示"无可用强制设备池设备" | 单测：空交集拦截 |
| S4.5 死字段清理 | `DispatchPlanGroup`/`DispatchPlanSummary`/`PreviewContext` | 删 `aliveInstanceIds`/`deadInstanceIds`；按 D-4 处置 `evidenceCapabilities`/`srcMethod105X`/`source`/`orderProcMethod` | 回归测试 |

**Wave S4 验收**：R-8/R-6/R-10；规模可控；死字段清零。

---

### Wave S5：文档同步 + 验收（P2）

| 任务 | 文件 | 改动要点 |
|------|------|----------|
| S5.1 字段说明文档 | `08-工具与指南/修复核验预览方案缓存字段说明.md` | 更新附录 A 死字段状态；新增 schemaVersion/vulId/assetCount/vulNum 等字段；标注 dependsOnGroupIndex 维持 deprecated |
| S5.2 技术设计文档 | `修复核验全链路-缓存一致性与设备分配技术设计-v1.0.md` | §5.9/§10.2/§12 标注被本波 PRD 收严/解答 |
| S5.3 文档地图索引 | `修复核验全链路-文档地图与索引` | 注册本 PRD + 开发计划；§4 状态更新 |
| S5.4 验收报告 | `06-Mock与联调/` | 缓存数据结构严谨化验收报告 + 全量 `mvn test` |

---

## 3. 依赖与顺序

```
S1.1(校验器) -> S1.2(接入) ─┐
S1.3(删资产兜底)            ├-> Wave S1 验收 -> S2.1(快照vulId) -> S2.2/S2.3 -> S2.4/S2.5
S1.4(漏洞收窄)              │                                            -> S2 验收
S1.5(切片精确)              │
S1.6(schemaVersion) ────────┘
S3（独立） / S4（S4.5 依赖 S1/S2 字段稳定）并行于 S2 之后
S5 收尾（依赖全部代码落地）
```

- S1.3/S1.4/S1.5 触及同一文件 `VulScanTaskSubDomainServiceImpl` / `ScanResultStageSlicer`，**同文件改动串行**，避免并行冲突。
- S2.1（快照 vulId）是 S2.2（group vulNum）/ S2.3（subTask vulNum）的前置。
- D-1~D-4 决策项需在对应 Wave 执行前定向。

---

## 4. 风险与回滚

| 风险 | 缓解 | 回滚 |
|------|------|------|
| S1.4 收窄 vulInstList 影响下游判定/台账 | S1.5 同步改切片；单测覆盖判定链 | 回滚 S1.4+S1.5（恢复构造器继承 + IP 切片） |
| S2.4 移除 targetScale 影响前端展示 | S2.4 前后端同提交 | 回滚字段+前端 |
| S3.1 确认复核增加下发延迟 | 计数校验（非逐条）；监控 P99 | 配置开关临时关闭复核（降级为仅 token 校验） |
| S4.1 体积护栏误拒正常大单 | 阈值可配；先压测真实大单体量定阈值 | 调高阈值 |
| schemaVersion 发布期在途 token 失效 | 30min 自然过期；发布窗口提示 | 可接受，无需回滚 |
| 死字段被外部模块反射/序列化依赖 | 清理前全局 grep 确认无消费 | 恢复字段 |

---

## 5. 测试索引

| 测试类 | 覆盖 | Wave |
|--------|------|------|
| `DispatchPlanIntegrityValidatorTest` | assetIps/instanceIds 越界、实例丢失/重复、候选池空 | S1 |
| `VulScanTaskSubDomainServiceImplTest`（补） | 资产过滤空抛异常、vulInstList 收窄、vulNum 按组 | S1/S2 |
| `ScanResultStageSlicerPrecisionTest` | 同 IP 跨 srcMethod 判定集互斥 | S1 |
| `CachedPlanSchemaVersionTest` | 版本不符拦截 | S1 |
| `VulInstSnapshotVulIdTest` | vulId round-trip | S2 |
| `DispatchConfirmRecheckTest` | stat 变更/设备离线拦截 | S3 |
| `ConfirmTokenSignerConfigTest` | 集群未配 tokenSecret 启动失败 | S3 |
| `DispatchPlanCacheSizeGuardTest` | 超阈值拒绝 | S4 |
| 全量 `mvn test` | 回归 | S5 |

---

## 6. 执行约定

- **未获「同意执行」前不派子 Agent、不改业务代码**（auto-dev-orchestrator）。
- 每个 Wave 完成后回归 `mvn test` + eslint（前端），再进入下一 Wave。
- 不主动 commit/push/建 PR，除非用户要求。
- 遵守 `java-hard-ban`/`backend-ddd-layers`/`backend-naming`/`security-hard-ban`/`test-required-strict`/`maven-dependency-strict` 等规则。
